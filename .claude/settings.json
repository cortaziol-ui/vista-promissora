{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd ~/Developer/outcom-dashboard && git add -A && git diff --cached --quiet || (git commit -m 'auto: claude edit' && git push)"
          }
        ]
      }
    ]
  }
}
